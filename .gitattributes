# Normalize line endings to LF in the repo, CRLF on checkout (Windows project)
* text=auto eol=crlf

# Binary files — no line ending conversion
*.png binary
*.ico binary
*.icns binary
*.msix binary
*.cer binary
*.exe binary
*.dll binary
